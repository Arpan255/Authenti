server:
  port: 6969
spring:
  application:
    name: security
  datasource:
    url: jdbc:mysql://mysqldoc3:3306/secu
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://eureka-a:8762/eureka/
